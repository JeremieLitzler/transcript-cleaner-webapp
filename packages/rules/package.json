{
  "name": "@transcript-cleaner/rules",
  "version": "0.1.0",
  "private": true,
  "description": "The transcript pipeline: level 1 reflow, the eleven level-2 rules, and the presets. No UI, no DOM, no runtime dependencies.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "check": "npm run typecheck && npm test"
  },
  "devDependencies": {
    "vitest": "^4.1.11"
  }
}
